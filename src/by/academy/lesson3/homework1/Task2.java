package by.academy.lesson3.homework1;

public class Task2 {
    public static void main(String[] args) {
        int a=8, b=5, c=7;
        double result;

        //z = ((a – 3 ) * b / 2) + c.
        result = (a - 3) * b / 2d + c;
        System.out.printf("((%s – 3 ) * %s / 2) + %s = %s\n", a, b, c, result);
    }
}
